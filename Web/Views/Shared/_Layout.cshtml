<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Flag/turkiye.css" rel="stylesheet" />
</head>
<style>
    #nav {
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        margin: 0;
        z-index: 100;
    }
</style>
<body>

    <nav class="navbar navbar-inverse" id="nav">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="@Url.Action("Index","Home")">Sepetim.com</a>
            </div>
            <ul class="nav navbar-nav">

                <a class="navbar-brand" href="@Url.Action("Index","Home")">Anasayfa</a>
            </ul>
            <ul class="nav navbar-nav">

                <a class="navbar-brand" href="@Url.Action("List","UserBasket")">Sepetim</a>
            </ul>
            <ul class="nav navbar-nav">

                <a class="navbar-brand" href="@Url.Action("ComletedList","UserBasket")">Siparişlerim</a>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="#"><span class="glyphicon glyphicon-user"></span>@Session["user"]</a></li>
                <li><a href="@Url.Action("LogOut","UserBasket")"><span class="glyphicon glyphicon-log-out"></span>Çıkış Yap</a></li>
            </ul>
        </div>
    </nav>

    @*<div class="row">
        <div class="col-md-10">
        </div>
        <div class="col-md-1">
            <br />

            <a href="@Url.Action("GetLanguage", "Home", new { id = "Türkce" })">
                <img src="~/Flag/turkbayragi.jpg" id="turk" />
            </a>
            <a href="@Url.Action("GetLanguage", "Home", new { id = "İngilizce" })">
                <img src="~/Flag/ingiliz.jpg" id="ing" />
            </a>
        </div>
        <div class="col-md-1">
            <br />
            <a href="UserBasket/List">
                <button class="btn btn-info" id="sepetim">
                    <span class="glyphicon glyphicon-shopping-cart"></span>
                    @if (Session["languageName"] == null)
                    {
                        <p>Sepetim</p>
                    }
                    else
                    {
                        <p>@Session["languageName"]</p>
                    }

                </button>
            </a>


        </div>
    </div>*@



    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
